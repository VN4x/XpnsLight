<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Block cleartext; app must use Caddy HTTPS endpoint -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- Debug builds: allow user-installed CA (Caddy tls internal) -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
